<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SensorMeasurementApp" parent="Theme.AppCompat.DayNight">
        <!-- Primary brand color. -->
        <item name="windowNoTitle">true</item>
       <!-- <item name="android:windowDrawsSystemBarBackgrounds">false</item> -->
    </style>

    <style name="MyToolbar" parent="ThemeOverlay.MaterialComponents.Toolbar.Primary">
        <item name="android:itemTextAppearance">@style/menuItem</item>
    </style>

    <style name="menuItem" parent="Widget.AppCompat.TextView.SpinnerItem">
        <item name="android:textColor">@color/white</item>
    </style>
</resources>